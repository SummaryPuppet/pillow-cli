[package]
name = "test"
version = "0.1.0"
edition = "2021"
readme = "README.md"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
pillow = { version = "*", features = ["templates"] }
tokio = { version = "1.23", features = ["macros"] }
